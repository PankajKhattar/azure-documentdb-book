services:
  documentdb:
    image: ghcr.io/documentdb/documentdb/documentdb-local:latest
    container_name: documentdb-local
    ports:
      - "10260:10260"
    command:
      - "--username"
      - "bookuser"
      - "--password"
      - "bookpassword"
    volumes:
      - documentdb_data:/var/lib/documentdb/data

volumes:
  documentdb_data: