services:
  documentdb:
    image: ghcr.io/microsoft/documentdb/documentdb-oss:0.1.0
    container_name: documentdb-oss
    ports:
      - "27017:27017"
    environment:
      POSTGRES_PASSWORD: postgres
    volumes:
      - documentdb_data:/var/lib/postgresql/data

volumes:
  documentdb_data:
